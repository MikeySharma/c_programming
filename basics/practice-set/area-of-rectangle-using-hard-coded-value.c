//A C program to calculate the area of rectangle using hard coded values.

#include <stdio.h>

int main() {
    int len = 23, breadth = 12;
    int area = len * breadth;
    printf("Area of rectange of length %d and breadth %d is %d.\n", len, breadth, area);
    return 0;
}